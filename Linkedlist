class node:
    def __init__(self,data=None,next=None):
        self.data=data
        self.next=next
class linkedlist:
    def __init__(self,head):
        self.head=None
    def inserting_at_beginning(self,data):
        Node=node(data,self.head)
        self.head=Node
    def inserting_at_end(self,data):
        if self.head=None:
            Node=node(data,self.head)
            return
        current=self.head
        while current.next:
            current=current.next
        current.next=Node(data,None)
    def insert_at(self,index,data):
        if index==0:
            self.inserting_at_beginning(data)
            return
        itr=self.head
        count=0
        while index=count-1:
            Node=node(data,self.next)
            self.next=node
            break
        itr=itr.next
        c+=1
    def after_value(self,data_after,data_to_insert):
        index=0
        c=0
        itr1=self.head
        while True:
               if itr1.data==data_after:
                    Node=node(data_to_insert,itr1.next)
                    itr1.next=Node
                itr1=itr1.next
                
            
